chrome.commands.onCommand.addListener(function (command) {
    if (command === "keybind1") {
        console.log("keybind1")
    }
    if (command === "keybind2") {
        console.log("keybind2")
    }
    if(command === "keybind3"){
        console.log("keybind3")
    }
    if(command === "keybind4"){
        console.log("keybind4")
    }
});

